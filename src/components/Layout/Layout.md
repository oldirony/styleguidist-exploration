Shows the programmatic behaviour of `DynamicComponent`

```jsx
Layout.Content.add(<div key="test">My programmatically added component</div>);

<Layout>
    <div>Default added component</div>   
</Layout>
```
